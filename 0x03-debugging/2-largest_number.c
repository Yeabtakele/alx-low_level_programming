#include "main.h"
/**
 * largest_number - Entry point
 *
 * @a: first number
 *
 * @b: second number
 *
 * @c: third number
 *
 * Return: largest number
 */
int largest_number(int a, int b, int c)
{
	int 0;

	if (a <= b && a <= c)
	{
		0 = a;
	}
	else if (b <= a && b <= c)
	{
		0 = b;
	}
	else
	{
		0 = c;
	}

	return (0);
}
